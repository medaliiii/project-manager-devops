apiVersion: v1
kind: Service
metadata:
  name: server
  labels:
    app: server
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  selector:
    app: server
  ports:
    - name: http
      port: {{ .Values.service.port | default 5000 }}
      targetPort: http
      protocol: TCP
